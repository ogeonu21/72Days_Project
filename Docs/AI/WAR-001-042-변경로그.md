# WAR-001 ~ WAR-042 변경 로그

> 작업 브랜치: `codex/war-001-to-042`  
> 기준선: `10ff8a7` (`wip: snapshot current project changes`)  
> 작성 시작: 2026-09-08

## 작업 원칙

- 각 작업은 구현 내용, 영향 범위, 검증 결과, 남은 수동 조치를 이 문서에 기록한다.
- Unity 자산·씬·프로젝트 설정은 필요한 경우에만 변경하고, 변경 후 Console과 Git diff를 확인한다.
- 비밀 값·키스토어 비밀번호·토큰은 기록하지 않는다.

## WAR-001 — 자격 증명 보호

### 변경 내용

- `.gitignore`에 `/Keystore/`, `*.keystore`, `*.backup`을 추가했다.
- Android 커스텀 키스토어 사용 설정을 비활성화해, 이 브랜치의 개발 빌드는 커스텀 서명 자격 증명에 의존하지 않도록 변경했다.

### 영향 범위

- `.gitignore`
- `ProjectSettings/ProjectSettings.asset`
- 다음 단계에서 Git 추적 중인 키스토어를 인덱스에서 제거할 예정이다. 로컬 파일은 유지한다.

### 검증/남은 조치

- 실제 키 회전·Play Console 자격 증명 교체·Git 이력 정리는 소유자 권한이 필요한 수동 작업이다.
- Android 개발 빌드로 설정 변경을 검증해야 한다.

## 이후 작업 기록

| 작업 ID | 상태 | 구현 요약 | 검증 | 남은 조치 |
| --- | --- | --- | --- | --- |
| WAR-002 | 예정 | 로컬/CI 서명 설정 문서화 | 미실행 | 안전한 비밀 저장소 결정 필요 |
| WAR-003 | 예정 | Android 템플릿/백업 산출물 정리 | 미실행 | Gradle 의존성 해석 확인 |
| WAR-010 | 완료 | DTO 기반 저장/복원 경로 연결, 노드·인벤토리·장비를 안정 ID로 변환 | Unity Console 컴파일 오류 0건 | 실제 새 게임 저장 후 불러오기 수동 확인 |
| WAR-011 | 완료 | 임시 파일 기록 후 교체하는 원자적 저장 적용 | Unity Console 컴파일 오류 0건 | 저장 실패/중단 상황 수동 확인 |
| WAR-012 | 완료 | 저장 버전 검증 및 유효하지 않은 노드·이전 Base64 저장 형식의 안전한 거부 처리 | Unity Console 컴파일 오류 0건 | 구버전 변환 정책 결정 |
| WAR-013 | 완료 | 새 게임 → 저장 → 불러오기 플레이 모드 검증 | `Main_01` 노드와 플레이어가 정상 복원되고 Console 오류 0건 | 장비·아이템을 보유한 세이브의 탐색 테스트 권장 |
| WAR-020 | 완료 | `SingleTon<T>`의 런타임 빈 GameObject 자동 생성을 제거하고 누락된 매니저를 명확히 오류로 표시 | 새 게임 전환 후 필수 매니저 각 1개, Console 오류 0건 | 장기적으로 `GameSession` 조립 루트로 대체 |
| WAR-021 | 완료 | `NodeRunner`가 현재 노드·메인 스토리 진행·스토리 선택 검증을 소유하고 UI는 선택 제출만 수행 | Play Mode에서 `Main_01 → Main_02` 진행, 잘못된 선택은 상태 변경 없이 거부 | 이벤트·전투 경로의 규칙 분리는 후속 작업 |
| WAR-022 | 완료 | `AttackArea` enum을 도입하고 GameWindow 공격 버튼 6개의 문자열 UnityEvent 인자를 정수 enum 값으로 교체 | Play Mode에서 enum 입력 전달 및 잘못된 값 거부 확인 | 효과·피해 규칙 분리는 WAR-023에서 진행 |
| WAR-023 | 완료 | 피해·명중·효과 여부를 순수 `CombatRules`로 추출하고 `CombatManager`는 난수 주입·코루틴 진행만 담당 | Unity 컴파일 오류 0건, 고정 입력에서 명중/피해 15/효과 및 빗나감 결과 확인 | 전체 전투 UI 경로의 수동 플레이 테스트 권장 |
| WAR-024 | 완료 | 미사용 `CombatEvent`, 빈 `EventNodeStart`, 폐기된 구 저장 주석 진입점을 제거 | Unity 컴파일 오류 0건, MCP 재연결 경고만 관찰 | 씬에 남은 빈 `StoryManager`는 직렬화 안전을 위해 후속 씬 정리 때 검토 |
| WAR-030 | 완료 | 스토리·이벤트 선택 버튼의 초기화/라벨/리스너 관리를 `ChoiceListPresenter`로 통합 | 새 게임 Play Mode에서 Story UI 활성화·버튼 3개·Console 오류 0건 | 이벤트 선택을 포함한 수동 UI 탐색 테스트 권장 |
| WAR-031 | 구현 및 기본 검증 완료 | NodeScreenRouter, HudView, PlayerStatsView, LevelUpModal로 책임 분리. UIManager 기존 Inspector 필드와 공개 버튼 API 유지. 이벤트 구독 해제 보완 | 2026-09-09 Unity 컴파일 오류 0건. 자동 저장을 끈 새 게임에서 Story 화면, 날짜 1 일차, STR 0 확인. 모달 반복 열기/닫기와 기존 시간 배율 복원 통과 | 전체 이벤트/전투/엔딩 UI 회귀와 기기 검증은 아직 미실행 |
| WAR-032 | 구현·기본 검증 | 메인/게임 CanvasSafeArea 연결, CameraResolution 화면 크기 변경 대응 | 계산 4종 통과, 새 게임 컨테이너 생성, Console 오류 0건 | 화면 이미지·실제 기기 검증 |
| WAR-040 | 구현·부분 검증 | 비동기 가져오기·30초 제한·전체 사전검증·건수 미리보기·GUID 보존·Undo 그룹 | 잘못된 응답 4종 거부 통과 | 온라인 실패·취소·Undo/Redo 실검증 및 상세 diff 미리보기 |
| WAR-041 | 구현·기본 검증 | 경로 포함 콘텐츠 오류 보고와 빌드 전 자동 검사 | 현재 콘텐츠 오류 0건, Android 빌드 전처리 통과 | 의도적 오류별 회귀 검사 보강 |
| WAR-042 | 개발 APK 빌드 성공 | 안전 영역·종료 구독 해제 수정까지 포함한 Android IL2CPP 개발 APK 생성 | 최종 60.96초, 오류 0건, 경고 1건 | 기기 실행·Resolver 단독 검사 |

## 2026-09-09 후속 기록

상세 결과와 남은 체크리스트는 `WAR-032-042-검증기록.md`, 개발/릴리스 서명 구분은 `Android-빌드-서명-가이드.md`에 기록했다.
MCP 재연결 후 안전 영역 씬 연결과 새 게임 기본 실행을 확인했다. 이전 표의 '완료' 표현은 해당 행의 남은 조치까지 검증되었다는 의미가 아니므로 릴리스 판정에는 각 완료 기준을 다시 대조해야 한다.

CombatManager의 종료 시 싱글턴 재검색 오류도 수정했다. 최종 APK는 `Builds/WAR042-verified-20260909/72Days-development.apk`다. 연결된 Android 기기가 없어 기기 검증은 미실행이다. Linear 도구가 현재 없어 외부 이슈 상태는 변경하지 않았다.
